name: Small Fix
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Find and Replace
        uses: jacobtomlinson/gha-find-replace@master
        with:
          find: "event.from_id"
          replace: "event.sender_id"
      - name: Create Pull Request
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: 'Fix for from_id'
          commit_options: '--no-verify'
          repository: .
          commit_user_name: sppidy
          commit_user_email: creatorofjarvis@gmail.com
          commit_author: Sρι∂у <58950863+xditya@users.noreply.github.com>
          commit_coauthor : xditya <58950863+xditya@users.noreply.github.com>
